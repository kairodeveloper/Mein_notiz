<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical">

        <TextView
            android:layout_margin="35dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Valor da média"
            android:layout_gravity="center"
            android:textSize="25sp"/>

        <com.gildaswise.horizontalcounter.HorizontalCounter
            android:id="@+id/settings_hc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:stepValue="1"
            android:layout_gravity="center"
            app:textColor="@color/colorButtom"
            app:minusButtonColor="@color/colorButtom"
            app:plusButtonColor="@color/colorButtom"
            app:displayInteger="true"
            app:initialValue="7"
            app:maxValue="8"
            app:minValue="6"
            app:textSize="10sp"/>

    </LinearLayout>

    <Button
        android:id="@+id/settings_button_clear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Limpar dados"
        android:layout_gravity="left|bottom"
        android:layout_marginLeft="10dp"
        android:onClick="clearAllDisciplines"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_save_setting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:src="@mipmap/ic_done"/>
    
</android.support.design.widget.CoordinatorLayout>

